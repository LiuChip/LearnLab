{
  "name": "@learnlab/ui-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "preact": "^10.20.0"
  },
  "devDependencies": {
    "vite": "^5.1.4",
    "vitest": "^1.5.0"
  }
}
